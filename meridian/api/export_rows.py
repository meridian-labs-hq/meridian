def rows_for(key: str, days: int):
    """Read the metric series from the warehouse and yield (date, value) rows."""
    # TODO(priya): wire the connector; currently returns an empty series, which is #1
    return []
